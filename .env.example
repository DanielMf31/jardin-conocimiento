# Jardin Conocimiento — example environment. Copy to .env and adjust; .env is git-ignored.
#
# SDS_HOME points at your clone of the software-development-system meta-repository. It is
# only needed by `make lint-structure`, which runs the structure linter that lives there.
# The Makefile assumes the meta-repository is a sibling of this project; override it here
# if it is not, or pass it inline: make lint-structure SDS_HOME=/path/to/sds
SDS_HOME=/home/danielmf31/Documentos/Documentos_Trabajo/Ingenieria/Programacion/VSCode/Proyectos_personales/software-development-system
