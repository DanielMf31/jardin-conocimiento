---
title: "C · Módulo 04 · Ej 02 — Funciones max(a,b) y min(a,b)"
date: 2026-08-05
tags: [programacion/c, curso/c, curso/c/ejercicio, dificultad/verde]
aliases: [c-m04-ej02]
type: espejo
status: espejo
source: mirror-generado
mirror: true
mirror_source: curso-c/practica/04-funciones/ej02.md
---

<!-- FICHERO GENERADO — NO EDITAR. Fuente de verdad: curso-c/practica/04-funciones/ej02.md (se regenera con gen_course.py). -->

# Ejercicio 02 — Funciones max(a,b) y min(a,b)

Dificultad: verde · Módulo 04 (Funciones)

## Enunciado

Funciones max(a,b) y min(a,b). Pide dos enteros e imprime el mayor y el menor.

## Diagramas de flujo

![Diagrama de flujo (actividad) del ejercicio 02](c-m04-ej02-actividad.png)

![Diagrama de flujo (grafo) del ejercicio 02](c-m04-ej02-grafo.png)

## Cómo se resuelve

<!-- EXPLICACION -->
Aquí escribes **dos funciones pequeñas**, `max(a, b)` y `min(a, b)`, cada una con su prototipo arriba. Cada una recibe dos enteros y devuelve uno solo.

- `max` compara: si `a > b`, `return a;` corta ahí mismo y sale de la función; si no llega a ese `return`, ejecuta `return b;`. No hace falta `else`, porque un `return` termina la función de inmediato.
- `min` es la misma idea con el operador al revés.
- En `main` lees dos enteros con `scanf("%d %d", &x, &y)` (los enteros **sí llevan `&`**, porque `scanf` necesita la dirección donde guardar) y pasas `x` e `y` a cada función.

El concepto clave es que `x` e `y` viajan **por valor**: dentro de `max`, los parámetros `a` y `b` son copias. La función devuelve un dato nuevo; no modifica tus variables originales.

Trampa típica: intentar que una sola función devuelva "el mayor y el menor a la vez". Una función con `return` normal devuelve **un único valor**. Para dos resultados, lo natural aquí son dos funciones separadas (más adelante verás cómo devolver varios valores con punteros).

## Para practicar — cópialo y complétalo

Pega este esqueleto y completa los `TODO`. Es la mejor forma de aprender: inténtalo antes de mirar la solución.

```c
/*
 * Curso de C — Modulo 04: Funciones
 * Ejercicio 02 — PRACTICA (rellena los TODO)
 * Enunciado: Funciones max(a,b) y min(a,b). Pide dos enteros e imprime el mayor y el menor.
 * Dificultad: verde
 * Compilar: gcc -std=c11 -Wall ej02_practica.c -o ej02 && ./ej02
 */

#include <stdio.h>

/* TODO: escribe los prototipos de max y min */

int main(void) {
    int x, y;
    /* TODO: pide dos enteros al usuario con printf + scanf */
    /* TODO: imprime el resultado de max(x, y) y de min(x, y) */
    return 0;
}

/* TODO: define int max(int a, int b)
 *   - devuelve el mayor de los dos
 *   - pista: un if simple basta
 */

/* TODO: define int min(int a, int b)
 *   - devuelve el menor de los dos
 */
```

## Solución — cópiala y ejecútala

```c
/*
 * Curso de C — Modulo 04: Funciones
 * Ejercicio 02 — MODELO (resuelto)
 * Enunciado: Funciones max(a,b) y min(a,b). Pide dos enteros e imprime el mayor y el menor.
 * Dificultad: verde
 * Compilar: gcc -std=c11 -Wall ej02_modelo.c -o ej02 && ./ej02
 */

#include <stdio.h>

/* Prototipos */
int max(int a, int b);
int min(int a, int b);

int main(void) {
    int x, y;
    printf("Introduce dos enteros: ");
    scanf("%d %d", &x, &y);
    printf("Max: %d\n", max(x, y));
    printf("Min: %d\n", min(x, y));
    return 0;
}

/* Devuelve el mayor de a y b */
int max(int a, int b) {
    if (a > b) return a;
    return b;
}

/* Devuelve el menor de a y b */
int min(int a, int b) {
    if (a < b) return a;
    return b;
}
```

## Cómo usarlo

Pega el código en un fichero y ejecútalo con tu toolchain habitual (o el botón de Ejecutar de tu editor). Antes de mirar la solución, intenta completar tú el esqueleto: es la mejor forma de aprender.

## Conexiones

- [[Curso_C/modelo/04-funciones|Teoría del módulo]]
- [[Curso_C/practica/00_README|Índice del curso]]
